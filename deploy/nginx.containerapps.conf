worker_processes auto;
pid /tmp/nginx.pid;
error_log /dev/stderr notice;

events { worker_connections 1024; }

http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;
  access_log /dev/stdout;
  sendfile on;
  client_body_temp_path /tmp/client_temp;
  proxy_temp_path /tmp/proxy_temp;
  fastcgi_temp_path /tmp/fastcgi_temp;
  uwsgi_temp_path /tmp/uwsgi_temp;
  scgi_temp_path /tmp/scgi_temp;

  server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location = /healthz {
      add_header Content-Type text/plain;
      return 200 'ok';
    }

    location = /api/ref {
      limit_except GET { deny all; }
      proxy_ssl_server_name on;
      proxy_ssl_name quiz.chatbot24.su;
      proxy_set_header Host quiz.chatbot24.su;
      proxy_set_header X-Forwarded-Proto https;
      proxy_pass https://quiz.chatbot24.su/ofeliya/api/ref;
    }

    location /api/ {
      proxy_ssl_server_name on;
      proxy_ssl_name quiz.chatbot24.su;
      proxy_set_header Host quiz.chatbot24.su;
      proxy_set_header X-Forwarded-Proto https;
      proxy_pass https://quiz.chatbot24.su/ofeliya/api/;
    }

    location = /runtime-config.js {
      add_header Cache-Control 'no-store' always;
      try_files $uri =404;
    }

    location /assets/ {
      add_header Cache-Control 'public, max-age=31536000, immutable' always;
      try_files $uri =404;
    }

    location = /index.html {
      add_header Cache-Control 'no-cache, must-revalidate' always;
      try_files $uri =404;
    }

    location = /ofeliya { return 308 /ofeliya/; }

    location = /ofeliya/runtime-config.js {
      rewrite ^/ofeliya/(.*)$ /$1 break;
      add_header Cache-Control 'no-store' always;
      try_files $uri =404;
    }

    location /ofeliya/assets/ {
      rewrite ^/ofeliya/(.*)$ /$1 break;
      add_header Cache-Control 'public, max-age=31536000, immutable' always;
      try_files $uri =404;
    }

    location /ofeliya/ {
      rewrite ^/ofeliya/(.*)$ /$1 break;
      add_header Cache-Control 'no-cache, must-revalidate' always;
      try_files $uri $uri/ /index.html;
    }

    location / {
      add_header Cache-Control 'no-cache, must-revalidate' always;
      try_files $uri $uri/ /index.html;
    }
  }
}
