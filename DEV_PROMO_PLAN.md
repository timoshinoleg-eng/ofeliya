# OFELIYA — оптимальный план развития и продвижения

Дата: 2026-09-08 · Основание: код `src/`, `RESEARCH_REPORT.md`, `RESEARCH_REPORT_EVALUATION.md`,
`opensource-scout-brief.md`. Фокус этапа: **визуал + играбельность**.

---

## 0. Новая сессия или продолжить в этой?

**Продолжай в этой — но с написанным handoff.**

Правило, по которому стоит делить сессии: **по эпику, а не по длине**.

| Ситуация | Что делать |
|---|---|
| Текущий эпик — визуал + играбельность (правим `GameScene`, `config.ts`, `UIScene`) | **Продолжать здесь.** Контекст загружен: ограничения MAX, файлы, баланс, выводы отчёта — всё «на виду». Новая сессия заставит перечитывать то же самое. |
| Переход к другому эпику: дуэли/бэкенд (Colyseus, seedrandom), аналитика (Umami), деплой | **Новая сессия.** Другой стек задач, контекст игры уже не нужен. |
| Признаки, что сессию пора закрыть | Ответы начинают повторяться, я «забываю» детали из начала, путаю файлы/числа. |

**Обязательное условие:** перед закрытием сессии — записать handoff (§7) и закоммитить.
Без него новая сессия потеряет контекст и начнёт переспрашивать уже решённое.

---

## 1. Аудит текущего состояния (проверено по коду)

### Уже есть — трогать не надо
| Что | Где |
|---|---|
| Bloom + vignette (postFX Phaser 3.60+, WebGL, Canvas-фоллбэк текстурой) | `GameScene.create`, `config.POSTFX` |
| Вспышка белым при попадании по врагу (70 мс) | `Enemy.preUpdate` / `takeDamage` |
| Тряска камеры на спавне/смерти босса, вспышка при уроне игроку | `spawnEnemy`, `onEnemyDied`, `onPlayerHit` |
| Частицы смерти (тинт по цвету врага) и подбора, blend ADD | `deathEmitter`, `pickupEmitter` |
| Нова-кольцо (твин), мигание в i-кадрах, мини-HP над игроком | `fireNova`, `update` |
| Хаптики + SFX + мьют | `MaxBridge`, `Sfx` |
| HUD: XP/HP/босс-бар, таймер, уровень, убийства | `UIScene.update` |
| Модалка левелапа (твин `Back.Out`), экран итога с 3 кнопками и шерингом | `UIScene` |

### Чего нет (и это чувствуется)
Нет **hit-stop**, **цифр урона**, **фанфар левелапа**, **предупреждения о низком HP**,
**иконок апгрейдов** (карточки текстовые), **онбординга**. Есть баг баланса босса (§2.1).

---

## 2. Играбельность — приоритет №1

### 2.1 P0: босс получает глобальный множитель сложности → ~8060 HP
`GameScene.spawnEnemy` для **всех** врагов, включая босса, применяет `difficulty()`.
`Enemy.activate`: `maxHp = def.hp * hpScale * (elite ? ELITE.hpMul : 1)`.

На 5:00: `hpScale = 1 + 5 × 0.42 = 3.1`, `dmgScale = 1 + 5 × 0.12 = 1.6`.

```
Босс: 2600 × 3.1 = 8060 HP   ·   урон 26 × 1.6 = 41.6 (при 100 HP игрока — 3 удара)
```
Игрок базово ~18 DPS; даже в сбалансированной сборке ~150–250 DPS → бой на 30–55 с
под давлением. **Винрейт у «не дамажных» сборок близок к нулю**, а победа = шеринг =
виральность. Это почти наверняка unintended (босс — фиксированный климакс, а не волна).

**Что сделать:** вынести босса из `difficulty()` (отдельный `bossHpScale` или ×1),
замерить винрейт, при необходимости подкрутить. Критерий: **побеждает 25–40% дошедших
до босса** — не 0%, но и не 100%.

### 2.2 Климакс не читается: на боссе спавн не снижается
`WaveDirector.update` продолжает обычный спавн после 5:00: интервал падает до 330 мс,
батч до 5 → **~15 врагов/сек** + босс + миньоны каждые 12 с. Игрок не видит босса.
**Снижать обычный спавн на время боя (×0.3–0.4)** — бой станет «дуэлью», а не кашей.

### 2.3 Производительность: кап 240 врагов
240 спрайтов + пули + частицы + bloom на среднем Android WebView — главный риск 60 FPS.
Группы уже пулятся (`maxSize`), но кап стоит проверить на реальном устройстве;
при просадках — 150 и отключение bloom флагом `POSTFX.enabled` (уже заложен).

### 2.4 Онбординг отсутствует
В мини-приложении мессенджера первые 20 секунд решают удержание. Нужно 3–5 сек:
«двигай джойстиком — оружие стреляет само» + подсветка джойстика, **только в первый
забег** (флаг в `SaveSystem`). Ни в `GameScene`, ни в `UIScene` этого сейчас нет.

### 2.5 `rollChoices` на `Math.random`
`UpgradeSystem.rollChoices` и `WaveDirector` (pickKind, ringPos) используют `Math.random` —
блокер для seeded-дуэлей. На этом этапе не трогать; заложить в спринт дуэлей
(плюс replay-тест детерминизма, не просто своп PRNG — см. `RESEARCH_REPORT_EVALUATION.md` §2.1).

---

## 3. Визуал — приоритет №2 (отдача на час работы)

| # | Что | Почему | Оценка | Где |
|---|---|---|---|---|
| 1 | **Hit-stop** — микрофриз 40–70 мс на убийстве/попадании | Самый дешёвый «мясной» фидбек; игра сразу «чувствуется» | 1–2 ч | `GameScene.update` (флаг паузы симуляции) |
| 2 | **Цифры урона** (всплывающие, пул текстов) | Читаемость прогресса; крит/обычный разными цветами | 2–3 ч | новый пул в `GameScene` |
| 3 | **Фанфары левелапа**: вспышка + radial burst + короткий slow-mo | Левелап — главная «награда» в жанре, сейчас только модалка | 1–2 ч | `UIScene.showLevelUp` + `GameScene` |
| 4 | **Предупреждение о низком HP** (<30%): пульсирующая красная рамка | Читаемость смерти; игрок понимает, почему умер | 1 ч | `UIScene.update` |
| 5 | **Тряска камеры** на уроне игроку и убийстве элита (сейчас только босс) | 30 мин, эффект заметный | 0.5 ч | `onPlayerHit`, `onEnemyDied` |
| 6 | **Иконки апгрейдов** (Kenney CC0 → реколор в неон) | Карточки сейчас текстовые; самый заметный UX-рост | 1 день | `UIScene.showLevelUp` + `UpgradeSystem` (поле `icon`) |
| 7 | **Комбо-счётчик** при серийных убийствах | Даёт цель в забеге, тянет «ещё разок» | 2 ч | `RunState` + `UIScene` |
| 8 | Трейл/afterimage игрока при движении | Дешёвое ощущение скорости | 1 ч | `Player` |
| 9 | Display-шрифт для заголовков вместо `Arial` | Arial убивает неон-стиль; осторожно с весом | 1–2 ч | `config.FONT` |

**Не брать сейчас:** Tone.js (75 КБ gzip — половина бюджета, только lazy-load и только
когда музыка реально нужна), rex-plugins (брать точечно, когда встроенного не хватит),
постэффекты сверх bloom/glow (упираемся в GPU мобильного вебвью).

---

## 4. Продвижение — фазы

**Фаза 0 · сейчас — «сделать достойным шеринга».** Никакого продвижения, пока винрейт
около нуля и нет «вау». Продвигать сырую игру = сжечь первый трафик. Визуал +
играбельность + винрейт 25–40%.

**Фаза 1 · виральный цикл MAX.** Главный рычаг — **seeded-дуэли**: `?startapp=duel:<seed>`
в чат → друг играет тот же забег → сравнение счёта. Плюс **share-карточка** (PNG на canvas:
счёт + ник + диплинк) — рисовать сами, готовые OG-генераторы тянут DOM/шрифты
(см. «дыры» в `RESEARCH_REPORT.md`).

**Фаза 2 · удержание.** Лидерборд (Node + SQLite на РФ-хостинге, тривиален),
еженедельные ивенты/модификаторы, стрики и напоминания через бота.

**Фаза 3 · масштаб.** Аналитика Umami self-host на РФ (MIT; **отключить сохранение IP** —
152-ФЗ), A/B обложки, масштабирование трафика.

**MAX-специфика, которую нельзя забыть:** ник бота генерируется автоматически
(`idИНН_bot`) и не меняется → бренд живёт **в тексте шеринга**, а не в ссылке.
Монетизация — фаза 0 = нет.

---

## 5. Порядок работ

**Спринт 1 (играбельность, ~1 день):** 2.1 босс → 2.2 спавн на боссе → 2.4 онбординг
→ замер винрейта на 10 забегах.
**Спринт 2 (визуал-фидбек, ~1 день):** пп. 1–5 из §3 (hit-stop, цифры, фанфары, low-HP, тряска).
**Спринт 3 (визуал-контент, ~1–2 дня):** пп. 6–9 (иконки, комбо, трейл, шрифт).
**Спринт 4:** профилировка на реальном Android → кап врагов / флаг bloom (2.3).
**Далее (новая сессия):** дуэли + share-карточка (Фаза 1).

Каждый спринт — коммит + прогон `tsc --noEmit`.

---

## 6. Критерии готовности этапа

- Винрейт среди дошедших до босса: **25–40%**.
- Бой с боссом длится **20–40 с** и читается (обычный спавн подавлен).
- 60 FPS на среднем Android WebView при пике врагов.
- Первый забег новичка: онбординг 3–5 с, понятно без слов.
- «Ещё разок» запускается из экрана итога без возврата в меню (уже есть).

---

## 7. Handoff для новой сессии

**Сделано:** проект = рогалик-выживание (Phaser 3.90 + TS strict + Vite, `base: './'`).
Есть `PLAN.md`, `RESEARCH_PROMPT.md` (v2), `RESEARCH_REPORT.md`,
`RESEARCH_REPORT_EVALUATION.md` (оценка отчёта), `opensource-scout-brief.md` (поисковый промт).
Хаб мини-игр — отдельный продукт в `hub/` (задеплоен в `timoshinoleg-eng/hub`), к Ofeliya
не относится.

**Известные ошибки в документах (не принимать на веру):** в `RESEARCH_REPORT.md` размер
Tone.js указан ~150 КБ gzip (реально **~75 КБ**); шкала скоринга в промте/брифе заявлена
0–10, фактически 0–36; postFX — WebGL-only, но **есть** WebGL1-fallback (не «требует WebGL2»).

**Дальше:** дуэли (seeded → ghost → Colyseus), share-карточка, лидерборд.
**Не трогать без нужды:** `Math.random` править только в спринте дуэлей (нужен replay-тест).
