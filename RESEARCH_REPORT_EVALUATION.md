# Оценка RESEARCH_REPORT.md (open-source для OFELIYA)

Дата: 2026-09-08 · Оценщик: анализ отчёта + выборочная проверка фактов (Phaser FX docs, Bundlephobia)

## Вердикт

Отчёт **сильный и пригодный к действию**. Головные выводы верны и хорошо обоснованы:
встроенные postFX Phaser — главный выигрыш (0 зависимостей), seedrandom — база для
seeded-дуэлей, Kenney CC0 — безопасные ассеты, Umami — MIT (не AGPL), Colyseus — для
real-time, Tone.js — тяжёлый, а geckos.io/nengi/Supabase — обоснованно исключены. Честно
заявлены «дыры» (пишем сами).

**Но есть 3 фактические/методологические ошибки и несколько недоработок**, которые стоит
исправить до того, как на отчёт будут опираться при внедрении.

---

## 1. Фактические ошибки (исправить обязательно)

### 1.1 Размер Tone.js занижен/завышен неверно — ~75 КБ gzip, а не ~150 КБ
- **Проверка (Bundlephobia, tone@15.1.22):** minified **329 КБ**, gzipped **74.8 КБ**.
  Доля — `standardized-audio-context` (36.7%) + сам Tone (62.7%).
- Отчёт пишет «бандл ~150 КБ gzip». Реально **~75 КБ gzip** (примерно в 2 раза меньше).
- **Следствие:** бюджет ≤150 КБ gzip вмещает Tone.js (75 КБ) **плюс** ещё пару небольших
  либ. Аргумент «только lazy-load, потому что съедает весь бюджет» был основан на
  удвоенной цифре. Lazy-load всё равно **рекомендую** (75 КБ — половина бюджета, грузить
  в меню, не в забег), но обоснование поправить.

### 1.2 postFX не «требует WebGL2» — он WebGL-only с WebGL1-fallback
- **Проверка (docs.phaser.io/concepts/fx):** FX — **«WebGL only feature … not available in
  Canvas mode as it relies on shaders»**. При этом Phaser 3.60+ имеет **fallback-шейдеры для
  WebGL1**; «WebGL2-only high-performance shaders» — *опциональны* (bloom/CRT/pixelation
  быстрее на WebGL2, но работают и на WebGL1).
- Красный флаг в брифе («постэффекты без WebGL1 fallback») **не точен**. Правильный риск:
  (а) **нет Canvas-фоллбэка** — если WebGL отключен целиком, postFX тихо не рисуются
  (в MAX WebView WebGL есть, но фоллбэк-флаг в `config.ts` всё равно нужен);
  (б) **стоимость на слабом GPU** — вот что надо профилировать, не совместимость.
- Топ-3 №1 (postFX + фоллбэк на per-object glow) — **верен**, просто перефразировать риск.

### 1.3 Шкала скоринга противоречива (0–10 заявлено, 0–36 используется)
- `RESEARCH_PROMPT.md` §6: «Оценка **0–10** = Влияние(0–4) × Перенос%(0–3) × Дешевизна(0–3)
  минус 2 за блокер». Произведение (4×3×3) даёт максимум **36**, не 10. Сам отчёт выдаёт
  «Скор» 36/27/24/18/18/12/6 — то есть на шкале **0–36**.
- Порог «≥4» из промта на шкале 0–36 бессмыслен (всё ≥4). Отчёт им и не фильтрует
  (PostHog=6 оставлен, ✕ только у явно исключённых).
- **Что сделать:** либо переименовать шкалу в 0–36 (и порог, напр. ≥10), либо
  нормализовать к 0–10 (поделить на 3.6). Та же ошибка попала в `opensource-scout-brief.md` §7
  — надо поправить в обоих файлах.

---

## 2. Недоработки / уточнения (важно для внедрения)

### 2.1 Детерминизм seeded-дуэлей недооценён
Отчёт: «заменить `Math.random` на seedrandom → 70% готово, движок уже детерминирован».
seedrandom — **необходимое, но недостаточное** условие. Полный кросс-устройственный
детерминизм требует аудита ВСЕХ источников недетерминизма:
- ни одного `Math.random`/`Date.now`/`performance.now` в симуляции;
- все спавны/выборы улучшений идут из одного seeded-PRNG в одинаковом порядке потребления;
- фиксированный dt (есть по отчёту) + детерминированная обработка ввода.
**Рекомендация:** вместо «просто свопнуть Math.random» — сделать **тест-обвязку
детерминизма**: записать последовательность ввода → replay → assert идентичный хеш
состояния на двух сборках. Это и есть реальный объём работы.

### 2.2 Umami: стоит ли ставить в фазе 0 + нюанс 152-ФЗ по IP
- В §2 Umami — кандидат №6 (скор 18), а в §4 — «выбор аналитики отложить до публикации».
  **Внутреннее противоречие.** Для фазы 0 (без монетизации) серверная аналитика не критична
  — отложить правильно; ранжировать Umami выше, чем «defer», не надо.
- **152-ФЗ:** Umami по умолчанию сохраняет **IP и User-Agent**. IP может считаться ПД →
  настраивать «Do not track IP» / анонимизацию при деплое на РФ-хостинг. Это дешевле, чем
  отказ от Umami, но должно быть в чек-листе деплоя.

### 2.3 Kenney CC0 — не drop-in под неон
Отчёт: «не заменить наш стиль, а дополнить; иконки 10 улучшений». Kenney — плоский/
рисованный, а Ofeliya — код-генерируемый неон (форма+обводка). Без реколора иконки выбьются
из стиля. **Рекомендация:** брать только стиль-нейтральное (спрайты взрывов, generic UI)
либо заложить **арт-пасс под неон-палитру (Lospec)** — иначе «самый заметный UX-рост»
обернётся визуальным диссонансом.

### 2.4 Категории I (метапрогресс) и J (референсы боссов) — тонкие
Бриф требовал их проработать; отчёт фактически свёл к «пишем сами». Для метапрогресса стоит
хотя бы указать типовые паттерны (версионируемые сейвы уже есть, unlock-tree, ачивки) и
хотя бы один репозиторий-референс по state-machine боссов. Не блокер, но пробел против брифа.

### 2.5 Протокол верификации не доведён до конца
Промт §5 требовал для топ-3: клонировать → `npm install && build` → прогнать демо →
приложить вывод. В отчёте этого нет (проверка «вручную по GitHub/сайтам»). Для топ-3
(postFX/Kenney/seedrandom) сборка тривиальна, но для Colyseus/Tone.js вывод сборки помог бы
оценить совместимость с Node 24 / Phaser 3.90. Минорно, но по протоколу — не выполнено.

### 2.6 Таблица: нет ссылок на файлы LICENSE
Промт §5(1) требует ссылку на файл LICENSE для каждого кандидата. В таблице — только ссылки
на репо. Для rex-notes/Umami заявлено «LICENSE подтверждён», но ссылки не приложены.

---

## 3. Что в отчёте верно и ценно (не трогать)

- postFX Phaser 3.60+ — единственный кандидат с 100% переносом и 0 зависимостей → брать первым.
- seedrandom (MIT, стабилен с 2019) — правильная база для дуэлей/дейли; исключение nengi
  (Node 14) и geckos.io («unreliable by design») обосновано.
- Kenney **CC0** (официально подтверждено) — безопасно по лицензии.
- Umami **MIT** (проверено, не AGPL) и PostHog core MIT — поправка против старой ошибки
  «всё AGPL».
- Colyseus (MIT, 7.3k★) — адекватен для real-time 1v1, но «вторая очередь».
- Исключение Supabase для одной таблицы рекордов — верно («из пушки по воробьям»).
- «Дыры» (survivors-референсы, ghost/daily-seed, share-карточка, лидерборд = пишем сами)
  — честная и правильная самодиагностика.

---

## 4. Приоритет исправлений

| Приоритет | Правка | Где |
|---|---|---|
| P0 | Tone.js gzip 75 КБ (не 150) | RESEARCH_REPORT.md §1 стр.7, §4 Tone.js |
| P0 | Шкала скоринга 0–36 (не 0–10) — поправить в промте и брифе | RESEARCH_PROMPT.md §6, opensource-scout-brief.md §7 |
| P1 | postFX-риск: WebGL-only + GPU-cost, не «нет WebGL1» | бриф §8 красный флаг + отчёт топ-3 №1 |
| P1 | Детерминизм: добавить replay-тест, не только своп PRNG | отчёт §2 дуэли A, бриф §4 A |
| P1 | Umami: defer в фазе 0 + отключать IP (152-ФЗ) | отчёт §2/§4 |
| P2 | Kenney: заложить неон-реколор-пасс | отчёт §2 №3 |
| P2 | Довести верификацию топ-3 (build-вывод) + ссылки LICENSE | отчёт §5/таблица |
| P2 | Категории I/J — хотя бы паттерны/референсы | отчёт §3/§4 |
